Пакет документации для разработки. См. также `Эпик7.txt` в этой папке (выжимка из Arch_TechDesign). Пути модулей: пакет `app` при рабочем каталоге `llm_p/` (рядом `pyproject.toml`).

---

# 🎯 Задание на разработку: EPIC7-TASK7.1 README и скриншоты для приёмки

## Контекст
Критерии оценки: без скриншотов указанных сценариев работа может не быть принятой.

## Текущее состояние
**Файл:** `README.md` в корне репозитория, папка `screenshots/` (опционально)  
**Функция/класс:** документация и артефакты демонстрации  
**Текущий вывод/поведение:** README минимальный или без скриншотов нужного регистра  
**Необходимый вывод/поведение:** полная установка через uv по ТЗ + скриншоты с email `student_surname@email.com` и ключевые эндпоинты

## Требования ТЗ
Файл `Разработка/Критерии_оценки_fastapi_llm.txt`: README, скриншоты register/login/token/authorize/POST chat/GET+DELETE истории.

## Задача
Оформить README и набрать все обязательные скриншоты под критерии.

## Технические требования

### 1. Документация сдачи
**Логика работы:**
1. Описание проекта, Python 3.11+, воспроизводимость через uv (инструкция из ТЗ + README).
2. Пример `.env` без ключей или с placeholders.
3. Команды запуска `uv run uvicorn` согласованы с тем, где лежит пакет `app`.
4. Вставить/сослаться на скриншоты: регистрация с нужным форматом email, логин и JWT, Authorize, `/chat`, `/chat/history` GET/DELETE.

### 2. Формат вывода
```
README включает ссылки на файлы `./screenshots/....png` либо встроенные изображения маркдауна.
```

**Где:**
- `student_surname@email.com` виден на нужных шагах критерия.

### 3. Обработка ошибок
```python
# Нет нужного скриншота
Повторить сценарий в Swagger аккуратно с подменой чувствительных данных перед публикацией.
```

## Файлы для изменения
- `README.md`
- `screenshots/*` если выносите картинки отдельно
- **Не изменять:** .env реальные секреты

## Пример реализации
```python
На этом этапе код не приводится. Реализация выполняется в отдельной итерации.
```

## Тестирование
не пишешь на этапе разработки

## Критерии приемки
- [ ] Критерии демонстрации из официального документа курса выполнены.
- [ ] Инструкция совпадает с реальной структурой репозитория.

## Примечания для разработчика
1. Перед публикации скрыть/замазать ключи если попали на скрин.

## Время на выполнение
Оценка: 3–5 часов (включая проверку по чек-листу задачи)

---

**Статус:** 🟢 Готово к разработке  
**Приоритет:** Высокий  
**Зависимости:** Эпик 6 (работающий сервер локально)

# 🎯 Задание на разработку: EPIC7-TASK7.2 Качество кода (`ruff`)

## Контекст
Критерии: `ruff check` без ошибок по проекту/пакетам приложения.

## Текущее состояние
**Файл:** весь `llm_p/app/**/*.py`, при необходимости конфиг линтера в `llm_p/pyproject.toml`  
**Функция/класс:** `ruff check`, при желании format  
**Текущий вывод/поведение:** есть нарушения стиля/правил ruff  
**Необходимый вывод/поведение:** из **`llm_p/`**: `uv run ruff check app/` (или `python -m ruff check app`) успешно; format рекомендован дополнительно

## Требования ТЗ
ТЗ финальное: выполнить `ruff check` и добиться сообщения успеха.

## Задача
Привести стиль импортов и очевидные замечания ruff к нулевой ошибке.

## Технические требования

### 1. Линтер
**Логика работы:**
1. Запустить check на приложении после реализации эпиков 1–6.
2. (Опционально) Применить `ruff format` — не смешать с функциональной логикой в одном коммите по желанию команды.
3. Не оставлять `print`/мёртвый код в финале.

### 2. Формат вывода
```
Вывод терминала: All checks passed!

```

**Где:**
- путь должен охватывать модуль `app` (рабочий каталог **`llm_p/`**).

### 3. Обработка ошибок
```python
# Ошибки ruff в чужих каталогах
Ограничить scope только `app` (из `llm_p/` или путём `ruff check llm_p/app`).
```

## Файлы для изменения
- `llm_p/pyproject.toml` (секции tool.ruff только если уже приняты в проекте)
- **Не изменять смысл** бизнес-логики — только стиль/import order при необходимости.

## Пример реализации
```python
На этом этапе код не приводится. Реализация выполняется в отдельной итерации.
```

## Тестирование
не пишешь на этапе разработки

## Критерии приемки
- [ ] `ruff check` зелёный для целевой области.
- [ ] Нет случайных отладочных print в финале.

## Примечания для разработчика
1. Если нужно игнорирование правила — согласовать с техлидом и задокументировать причиной в комментарии минимально.

## Время на выполнение
Оценка: 1–2 часа (включая проверку по чек-листу задачи)

---

**Статус:** 🟢 Готово к разработке  
**Приоритет:** Высокий  
**Зависимости:** Эпики 1–6

# 🎯 Задание на разработку: EPIC7-TASK7.3 Ручное тестирование сценариев перед сдачей

## Контекст
Проследовать полному сценарию из методички: регистрация → логин → JWT → авторизация в Swagger → LLM запрос → история.

## Текущее состояние
**Файл:** корневой `README.md` (раздел проверки), заметки; инструменты curl/Swagger/sqlite3  
**Функция/класс:** manual QA чеклист  
**Текущий вывод/поведение:** сценарий не воспроизведён документально  
**Необходимый вывод/поведение:** зафиксировано прохождение всех пунктов задачи и соответствие критике «без токена → 401»

## Требования ТЗ
ТЗ: шаги запуска, Swagger authorize, история сохранена в SQLite.

## Задача
Провести сквозной сценарий и зафиксировать результаты (краткий чеклист в README можно).

## Технические требования

### 1. Ручное тестирование
**Логика работы:**
1. Запуск из чистого venv через uv как в документации.
2. Регистрация + логин + копирование JWT.
3. Authorize в Swagger, вызовы защищённых методов без 403 лишним.
4. `POST /chat` с ключом OpenRouter получает текст от модели или корректно отображает ошибку сервиса без падения.
5. История содержит user+assistant сообщения; DELETE очищает.
6. `sqlite3 app.db` из **`llm_p/`** — sanity check записей если нужно проверке.
7. Без токена все защищённые методы возвращают ожидаемый refuse (401).

### 2. Формат вывода
```
Табличный или буллетный QA чеклист в README после прохождения.
```

**Где:**
- результат каждого шага ✅/⚠️ с пояснением если LLM временно недоступен через OpenRouter но ошибка управляемая

### 3. Обработка ошибок
```python
# OpenRouter rate limit/network
Подтвердить что ошибка доменная и понятная пользователю API без 500 необработанного.
```

## Файлы для изменения
- `README.md` (раздел QA)
- SQLite CLI при необходимости

## Пример реализации
```python
На этом этапе код не приводится. Реализация выполняется в отдельной итерации.
```

## Тестирование
не пишешь на этапе разработки

## Критерии приемки
- [ ] Сквозной сценарий пройден согласно ТЗ.
- [ ] 401 без токена подтверждён для chat/history методов.
- [ ] История не смешивает пользователей (проверить двумя разными аккаунтами по возможности).

## Примечания для разработчика
1. При демонстрации преподавателю иметь активный ключ и стабильный интернет или заранее заготовить скрины.

## Время на выполнение
Оценка: 2–3 часа (включая проверку по чек-листу задачи)

---

**Статус:** 🟢 Готово к разработке  
**Приоритет:** Высокий  
**Зависимости:** Эпики 6–7.1 завершённые функционально

